suites:
    searchindex_suite:
        namespace: Spatie\SignedUrl
        psr4_prefix: Spatie\SignedUrl
        src_path: src

extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format:
    - clover
  output:
    clover: build/logs/clover.xml
